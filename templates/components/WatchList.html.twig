<div{{ attributes }}>
    	<button class="btn" data-action="live#action" data-action-name="toggle">
		{% if app.user %}
			{% if app.user.watchlist.contains(this.program) %}
				<i class="bi bi-heart-fill"></i>
			{% else %}
				<i class="bi bi-heart"></i>
			{% endif %}
		{% endif %}
	</button>
</div>
